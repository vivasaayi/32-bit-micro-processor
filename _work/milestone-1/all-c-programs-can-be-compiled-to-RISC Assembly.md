# Goal
We have a lot of C programs which we used to compile to Assembly and test using our micro processor.

We need to make sure they are all organized correctly, so that we can compile them to RISCV Assembly using our AruviAsm and execute them.